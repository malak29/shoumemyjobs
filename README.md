# shoumemyjobs
Small static page that builds **LinkedIn job search URLs** for the **United States** and selected **states** (software engineer search, with optional time and company filters). Each button opens the generated URL in a **new tab**.

Open `LinkedInJobs.html` in a browser.

